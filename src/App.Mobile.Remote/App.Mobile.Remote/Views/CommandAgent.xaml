<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:viewModels="clr-namespace:App.Mobile.Remote.ViewModels;assembly=App.Mobile.Remote"
             xmlns:code="clr-namespace:App.Mobile.Remote.Code;assembly=App.Mobile.Remote"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance viewModels:AgentViewModel}"
             x:Class="App.Mobile.Remote.Views.CommandAgent">
	<ContentPage.Behaviors>
		<code:AppearingActivatorBehavior />
	</ContentPage.Behaviors>
	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition  Height="Auto"/>
			<RowDefinition  Height="*"/>
		</Grid.RowDefinitions>

		<Label Text="{Binding Path=AgentName}" />

		<ListView ItemsSource="{Binding Path=Commands}"
				  Grid.Row="1"
		          CachingStrategy="RecycleElement">

			<ListView.ItemTemplate>
				<DataTemplate>
					<TextCell Text="{Binding Path=Text}" Command="{Binding Path=Command}" />
				</DataTemplate>
			</ListView.ItemTemplate>
		</ListView>
	</Grid>
</ContentPage>
